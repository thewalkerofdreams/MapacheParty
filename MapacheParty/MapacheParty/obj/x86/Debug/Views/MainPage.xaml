<Page
    x:Class="MapacheParty.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MapacheParty"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:MapacheParty.ViewModels"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <vm:ClsMainPageVM></vm:ClsMainPageVM>
    </Page.DataContext>

    <StackPanel>
        <StackPanel Orientation="Horizontal" Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:ConnectionId='6' x:Name="txtJugador1" Text="Jugador1 --> "></TextBlock>
            <TextBlock x:ConnectionId='7' x:Name="MonedasJugador1" Text="{Binding MonedasJugador1}"></TextBlock>
            <TextBlock Text=" monedas"></TextBlock>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Margin="10" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:ConnectionId='4' x:Name="txtJugador2" Text="Jugador2 --> "></TextBlock>
            <TextBlock x:ConnectionId='5' x:Name="MonedasJugador2" Text="{Binding MonedasJugador2}"></TextBlock>
            <TextBlock Text=" monedas"></TextBlock>
        </StackPanel>
        <GridView x:ConnectionId='2' x:Name="gridView01" ItemsSource="{Binding Tablero}" SelectedItem="{Binding CasillaSeleccionada, Mode=TwoWay}" HorizontalAlignment="Center" VerticalAlignment="Center">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <Image Source="{Binding Imagen}" Width="100" Height="100"></Image>
                </DataTemplate>
            </GridView.ItemTemplate>
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid MaximumRowsOrColumns="5" Orientation="Horizontal"></ItemsWrapGrid>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
        </GridView>
    </StackPanel>
</Page>

